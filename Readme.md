Step 1 - Install Gulp Cli globally
npm install --global gulp-cli

Step 2 - Install project modules
npm install

To work with assets you must use gulp
run `npm start`
all distribution files are in ./dist
